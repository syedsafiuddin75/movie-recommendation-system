# movie-recommendation-system
This Machine Learning project recommends movies to users based on ratings and similarity.  The program:  Uses a built-in movie dataset Processes movie ratings Finds similar movies Recommends movies automatically Displays recommendation results
